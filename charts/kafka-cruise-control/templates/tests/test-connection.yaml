apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka-cruise-control.fullname" . }}-test-connection"
  labels:
    {{- include "kafka-cruise-control.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kafka-cruise-control.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
